import {type SharePostImageDialogProps} from './SharePostImageDialog.types'

/** This export is web-only; native retains the existing platform share flow. */
export function SharePostImageDialog(_props: SharePostImageDialogProps) {
  return null
}
