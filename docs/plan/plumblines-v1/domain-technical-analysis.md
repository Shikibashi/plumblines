# Technical analysis
Client runtime owns presentation and local capability checks; PDS/AppView owns actual remote state/enforcement. Query mutation is mandatory policy boundary; UI hiding is supplementary. ALF is theme composition point. Web layout and native variants share data, so web-only typography should be platform scoped. Existing tests and package scripts are verification harness.
