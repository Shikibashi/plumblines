# Synthetic interview (Zeus automatic decisions)
This is inferred, not a record of user answers.

C: The user wants a real fork of Bluesky matching their newspaper reference. Existing fork setup is a prior milestone.
P: Reader task: navigate existing feeds, read/reply/post and manage attention without block creation. Success: actual app renders the style and existing protocol features remain usable.
S: Reuse client routes/query hooks; isolate fork configuration and policy; apply ALF theme and desktop composition changes.

Gate 1: Client only; AppView/PDS remain external unchanged dependencies.
Gate 2: No block or listblock creation; existing-state removal remains available.
Gate 3: Accept bounded web-first implementation and local evidence; do not claim native/store/production or credentialed acceptance without execution.

[ZEUS-AUTO:taste] Screenshot is approved direction | reason: explicit “make it look like this” | alternative: three mockup directions | reversal: change DESIGN.md before another visual iteration.
[ZEUS-AUTO:taste] System serif stack avoids new font dependency | reason: maintainable first pass | alternative: licensed web font | reversal: add verified font via existing font loader.
