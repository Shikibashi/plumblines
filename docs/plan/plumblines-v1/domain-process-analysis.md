# Process analysis
Guest/member navigates existing route tree. Following or custom feed query resolves through existing client. Render shell context from active feed, not a static claim. Member attention action is classified by policy: new block/listblock denied; mute/report existing behavior; remove existing block allowed. Network failures retain upstream error handling.
